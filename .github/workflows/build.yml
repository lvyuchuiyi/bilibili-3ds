name: Build BiliBili 3DS

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkitarm:latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install makerom if missing
      run: |
        dkp-pacman -S --noconfirm general-tools 3ds-dev 2>&1 || echo makerom install failed, will use fallback
    
    - name: Build .3dsx and .cia
      run: |
        make release -j$(nproc)
        echo "=== Output files ==="
        find . -name "*.3dsx" -o -name "*.cia" -o -name "*.smdh" 2>/dev/null
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: bilibili3ds-build
        path: |
          *.3dsx
          *.cia
          *.smdh
          build/*.3dsx
          build/*.cia
          build/*.smdh
        if-no-files-found: warn

